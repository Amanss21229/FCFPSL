## Packages
jspdf | Client-side PDF generation
jspdf-autotable | Table support for PDF generation
framer-motion | Smooth animations for buttons and page transitions
lucide-react | Icons (already in base but ensuring version match)
xlsx | For exporting student data to Excel

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
